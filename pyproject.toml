[project]
name = "portfolio-market-data"
version = "0.2.0"
description = "Off-chain market price and Getquin transaction loaders"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "lxml>=6.0.2",
    "mstarpy>=11.0.1,<12",
    "pandas>=2.3.3",
    "portfolio-core @ git+https://github.com/T1m-V/portfolio-core.git@v0.2.0",
    "requests>=2.32.5",
    "yfinance>=1.0",
]

[project.scripts]
portfolio-market = "portfolio_market_data.cli:main"

[dependency-groups]
dev = ["pytest>=9.0.2", "ruff>=0.14.10"]

[build-system]
requires = ["uv-build>=0.12.3,<0.13"]
build-backend = "uv_build"

[tool.uv]
required-version = ">=0.12.3,<0.13"
preview-features = ["centralized-project-envs"]

[tool.uv.sources]
portfolio-core = { path = "../portfolio-core", editable = true }

[tool.ruff]
line-length = 100
target-version = "py314"
src = ["src"]

[tool.ruff.lint]
select = ["E", "W", "F", "I"]
